La estructura del proyecta ya está construída, la lógica es bastante simple:
el proyecto tiene una tabla dinámica que tiene todos los partidos, y una columna de pacto en la que el usuario puede ingresar el nombre de un pacto para cada partido.
Luego cuando se presione el botón calcular, el df que el usuario definido ingresando datos en el partido se pasa por la función calcular que es está basada en la función
Calcula_integracion_diputados.py. 
A esta última función aún hay que modificarle algunas cosas, en particular adaptar la función a la variable de entrada y definir la variable de salida para que pueda ser leída por el visualizado y al visualizador hay que integrarle una tabla que muestre los resultados del cálculo para ese partido